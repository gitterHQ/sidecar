dependencies:
  cache_directories:
    - browserstack
test:
  override:
    - ci/browserstack_tunnel.sh
    - browserstack/BrowserStackLocal -skipCheck -f $BS_KEY:
        background: true
    - mocha
    - ci/browserstack_tunnel.sh stop
